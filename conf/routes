# Routes For the Application

# /families endpoints
GET     /families/:email       	controllers.FamiliesController.findFamily(email : String)
POST    /families/add          	controllers.FamiliesController.save

# /users endpoints
GET 	/users/auth				controllers.UsersController.getUser